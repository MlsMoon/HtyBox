# 更新日志

---

## 2026-07-27

- 新增了「内容预览窗口」：可从顶栏开出的独立第二窗口，打开的文件、SVG、图片都进它，主窗口只留终端
- 支持了预览窗口按工作区各自记忆开关状态，切换工作区自动开启或收起，退出应用重启后也会连同标签一起复原
- 支持了打开预览窗口时把主窗口已开的编辑器标签整体搬过去，未保存的改动一并带走
- 支持了在预览窗口里按 Ctrl+P 或双击 Shift 搜索当前工作区的文件并直接打开
- 优化了 Markdown 预览的配色：表头有底色、引用有色条、行内代码与列表标记分色，代码块用上了与 VSCode 同款的语法高亮
- 新增了代码文件的预览视图：带行号的只读语法高亮，覆盖 TypeScript / Rust / C# / Go / Java / Python / Vue 等常用语言
- 优化了 Markdown、SVG 与代码文件打开时默认进入预览，想改再切到编辑
- 优化了预览里代码字体，跟随设置中的界面字体，不再另用一套字体
- 新增了「标签页可选中」开关（默认关闭）：关闭时点标签只切换并立即聚焦终端输入，可直接打字，也不会误按删除键关掉终端
- 修复了 Markdown 预览里点击链接会让整个窗口跳走的问题，现在外部链接交给系统浏览器，工作区内的文件在当前窗口打开
- 修复了点击标签页上的 ✕ 会先切到该标签再关闭导致的闪烁
