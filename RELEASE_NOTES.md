# HtyBox v1.5.3

## 2026-07-07

- 优化了 SVG 预览:写坏的注释、未转义的 & 等常见瑕疵会自动容错渲染出图,不再动辄弹「无法预览」报错逼手动改文件,且只作用于预览、不改动原始文件
- 优化了全局搜索定位：文件树还没刷新时（比如终端里刚新建的文件），点击搜索结果也能直接在树里展开并滚动定位，不用先手动刷新一次

## 2026-07-06

- 修复了在全局搜索里重复定位同一个文件时，文件树不会重新滚动聚焦到它的问题（原先需先点开别的文件才能再次定位）
