# HtyBox v1.6.1

## 2026-07-10

- 新增了在全局搜索中直接收藏文件夹：搜索结果里的文件夹行点爱心即可收藏，深层文件夹不必在树里逐级展开
- 支持了收藏文件夹在改名、移动、删除后自动跟随更新，不再残留失效的收藏项
- 支持了收藏文件夹的拖拽：可拖进终端注入路径，也可把文件拖到收藏项上移入该文件夹
- 改进了取消收藏的方式：从悬停按钮改为右键菜单，避免误触
- 新增了截图 / 图片粘贴：终端里 Ctrl+V 即可把剪贴板截图发给 Claude / Codex，图片存入工作区 .htybox/tmp 并以 @路径 引用注入（48 小时自动清理旧图）
- 新增了工作流输入框的图片附件：粘贴的截图先以附件形式暂存（可查看、可删除，删除即删文件），随文字一起发送，不会出现"提示与终端不一致"的假状态
- 修复了在弹窗内按住鼠标（如选中文字）拖到窗口外松开导致弹窗被误关、输入内容丢失的问题，设置、工作流编辑、确认框等全部弹窗与下拉均生效
- 修复了文件树多选时从文件行按住拖到空白处松开会误清选中的问题
